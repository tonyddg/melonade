#let bm(x) = math.upright(math.bold(x))

#let github-raw-root = "https://github.com/tonyddg/melonade/blob/main/"
// 给出基于 github 仓库的路径，ref-path 为文件基于项目根目录的路径
#let github-raw-link(ref-path) = github-raw-root + ref-path